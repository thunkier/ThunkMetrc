from typing import TypedDict, List, Optional, Any, Generic, TypeVar, TYPE_CHECKING
from typing_extensions import TypedDict

if TYPE_CHECKING:
    from . import *

class {{ .Name }}(TypedDict, total=False):
{{- if .Properties }}
    {{- range .Properties }}
    {{ .JSONName }}: {{ ToPythonType .Type }}
    {{- end }}
{{- else }}
    pass
{{- end }}
